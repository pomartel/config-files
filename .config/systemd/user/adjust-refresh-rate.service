[Unit]
Description=Monitor and adjust refresh rate based on power profile
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/bin/adjust-refresh-rate
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
