general {
    lock_cmd = omarchy-lock-screen                         # lock screen and 1password
    before_sleep_cmd = loginctl lock-session               # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on             # to avoid having to press a key twice to turn on the display.
    inhibit_sleep = 3                                      # wait until screen is locked
}

listener {
    timeout = 300 # 5min
    on-timeout = sh -c 'if ! hyprctl monitors | grep -q "monitor mirrors"; then pidof hyprlock || omarchy-launch-screensaver; fi'
}

listener {
    timeout = 300 # 5min
    on-timeout = brightnessctl -sd *:kbd_backlight set 0 # turn off keyboard backlight
    on-resume = brightnessctl -rd *:kbd_backlight # turn on keyboard backlight
}

listener {
    timeout = 600 # 10min
    on-timeout = loginctl lock-session # lock screen when timeout has passed
}

listener {
    timeout = 630                                            # 10.5min
    on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r # screen on when activity is detected
}
