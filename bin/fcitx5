#!/bin/bash

# Stub for fcitx5 (intentionally does nothing)
# This bin is required so Omarchy autostart.conf
# doesn't throw an error on startup. This package
# required by XCompose which is no longer used.
# If needed, fcitx5 can be reinstalled via:
# sudo pacman -S fcitx5-qt fcitx5-gtk fcitx5

exit 0
