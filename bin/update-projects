#!/bin/bash

echo "Pulling yadm..."
cd ~ || exit
yadm pull

echo "Pulling markdown..."
cd ~/Work/markdown/ || exit
git pull

echo "Pulling Install..."
cd ~/Install/ || exit
git pull
source INSTALL.sh
