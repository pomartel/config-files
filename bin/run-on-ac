#!/bin/bash

cmd=$1

if [ -z "$cmd" ]; then
  echo "Usage: run-on-ac <command> [args...]"
  exit 1
fi

if omarchy-cmd-missing "$cmd"; then
  echo "Error: \"$cmd\" not found."
  exit 2
fi

shift

# Execute the command if on AC power
if ! batery-discharging; then
  "$cmd" "$@"
fi
