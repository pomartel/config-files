#!/usr/bin/env bash
set -euo pipefail

volume_down() {
  omarchy audio output volume lower
}

volume_up() {
  omarchy audio output volume raise
}

case "${1:-}" in
down)
  volume_down
  ;;
up)
  volume_up
  ;;
*)
  echo "usage: $0 down|up" >&2
  exit 2
  ;;
esac
