If you are working in a git repo or if the files are tracked in yadm, always commit and push and show me the diffs.

If you are working on a plugin, always modify the code in ~/Projects and also the live plugin code. Reload the plugins with `omarchy restart shell`. 


When I ask you to manage my reminders and todo, use the Todoist MCP.

`~/Install` contains all the bash script to sync apps and configs between my two computers and do a fresh Install. When I ask you to add something to my install script, do it while respecting the format and organization of the project. It's idempotent and if nothing is installed, nothing is echoed.